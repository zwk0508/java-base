# 方法调用

```java

//方法调用一般格式
retType ret = receiver.methodName(argList);

//retType 返回值类型
//ret 返回值变量
//receiver方法接收者
//methodName方法名
//argList参数列表
//无返回值方法调用
receiver.methodName(argList);
//简单的例子

double d = Math.pow(2, 4);//计算2的四次方

```